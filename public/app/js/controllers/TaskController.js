'use strict';

kanbanApp.controller('TaskController',
    function TaskController($scope, boardData, containersData, $http) {
        var boardId = 1;
        
        console.log("TaskController!");
        
        
    }
);